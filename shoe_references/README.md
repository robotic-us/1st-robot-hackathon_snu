# Shoe reference gallery

Add 3–6 real overhead images for each known shoe:

- `white_pair_object1/` — provisional right white shoe
- `white_pair_object2/` — left white shoe
- `brown_pair_object3/` — brown shoe, side unknown

Use the real camera height, angle, floor, and lighting when possible. Include
several rotations and keep only one shoe in each image. Until these directories
contain images, `vision_shoe_test.py` falls back to each scan's `3DModel.jpg`.
